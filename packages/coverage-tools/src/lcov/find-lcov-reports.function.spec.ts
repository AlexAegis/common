import { describe, expect, it } from 'vitest';

describe('findLcovReportPaths', () => {
	it('should return paths of all lcov reports in the workspace except at the root', () => {
		expect(1).toBe(1);
	});
});
